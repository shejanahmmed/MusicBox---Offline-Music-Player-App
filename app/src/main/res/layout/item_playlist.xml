<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_track_card"
    android:padding="12dp"
    android:layout_marginBottom="12dp">

    <androidx.cardview.widget.CardView
        android:id="@+id/cv_playlist_thumb"
        android:layout_width="40dp"
        android:layout_height="40dp"
        app:cardCornerRadius="4dp"
        app:cardElevation="0dp"
        app:cardBackgroundColor="#1AFFFFFF"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <!-- Thumbnail / Background -->
        <ImageView
            android:id="@+id/iv_playlist_thumb"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_queue_music"
            android:padding="8dp"
            app:tint="@color/text_white_opacity_40" />

        <!-- Special Icon (Heart for Favorites) -->
        <ImageView
            android:id="@+id/iv_special_icon"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_heart"
            app:tint="@color/white"
            android:visibility="gone" />

    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/tv_playlist_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Playlist Name"
        android:textColor="@color/white"
        android:textSize="12sp"
        android:textStyle="bold"
        android:textAllCaps="true"
        android:maxLines="1"
        android:ellipsize="end"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="8dp"
        app:layout_constraintStart_toEndOf="@id/cv_playlist_thumb"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/iv_chevron"
        app:layout_constraintBottom_toTopOf="@+id/tv_track_count"
        app:layout_constraintVertical_chainStyle="packed"/>

    <TextView
        android:id="@+id/tv_track_count"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="0 tracks"
        android:textColor="@color/text_white_opacity_40"
        android:textSize="9sp"
        android:textAllCaps="true"
        android:letterSpacing="0.1"
        android:maxLines="1"
        android:ellipsize="end"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="8dp"
        android:layout_marginTop="2dp"
        app:layout_constraintStart_toEndOf="@id/cv_playlist_thumb"
        app:layout_constraintTop_toBottomOf="@id/tv_playlist_name"
        app:layout_constraintEnd_toStartOf="@+id/iv_chevron"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <ImageView
        android:id="@+id/iv_chevron"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/ic_chevron_right"
        app:tint="@color/text_white_opacity_40"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
